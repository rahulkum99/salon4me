- name: Deploy to EC2
  uses: appleboy/ssh-action@v0.1.0
  with:
    host: ${{ secrets.EC2_HOST }}  # Ensure EC2_HOST is set in GitHub Secrets as a string
    username: ubuntu               # Ensure the correct username for your EC2 instance
    key: ${{ secrets.EC2_SSH_KEY }}  # The private key should be set as a string secret in GitHub
    script: |
      echo "Connected to EC2 instance successfully"
      # Add your deployment commands here

